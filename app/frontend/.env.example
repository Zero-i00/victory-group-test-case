NODE_ENV=development

# Base
PORT=3000
HOST=localhost
TITLE="DentistAm"

# API
# Публичный URL — используется браузером (резолвится с хоста через port-mapping)
NEXT_PUBLIC_SERVER_URL='http://localhost:8080'
# Внутренний URL — используется SSR внутри docker-сети (имя сервиса)
SERVER_URL_INTERNAL='http://backend:8080'



