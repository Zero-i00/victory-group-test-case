services:
  backend:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./static:/app/static
    ports:
      - "${PORT}:${PORT}"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
